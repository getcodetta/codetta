# Canonical author for every commit reachable from main. Reachable
# history is already authored by `getcodetta` (older commits with
# personal emails were rewritten in a previous filter-branch and only
# survive in local backup refs that never reached origin). Listing the
# canonical entry here keeps the project's identity declared
# explicitly so future contributions stay coalesced.
#
# Format reference: https://git-scm.com/docs/gitmailmap

getcodetta <280564977+getcodetta@users.noreply.github.com>
